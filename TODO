(DONE)
allow unselecting a ball
enable exit game
add replay
add undo
ensure only one ball per position
keypress helper
make checks keyboard
redo move
allow or disallow launching a ball on a direct neighbor
darker balls + texture
when solving, draw lower resolution balls
pause exploration after time
implement solver
load/save file

(GOOD)
factorize apply move
clean the code
smooth ball movement
redraw grid
hist + fwd are displacement list list
add doc-related files to make compress
clear queued events

(TODO)
automatic game generation
refactor undo_move/redo_move + pop_hist/pop_fwd ?
refactor character input in game::get_ball
criterion to trim the tree
update doc
